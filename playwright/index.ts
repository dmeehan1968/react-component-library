// Import Tailwind v4 + DaisyUI styles for Playwright Component Testing.
// This ensures layout/styling matches the app build so CT can validate UI.
import '../src/index.css';

// If you add global tokens or CSS variables, keep them in `src/index.css` as per guidelines.
